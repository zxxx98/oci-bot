services:
  oci-bot:
    image: ghcr.io/<github-owner>/oci-bot:latest
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      DATA_DIR: /app/data
      OCI_DIR: /root/.oci
    volumes:
      - /your/local/data:/app/data
      - /your/local/oci:/root/.oci
    restart: unless-stopped
